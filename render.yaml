services:
  - type: web
    name: community-pulse-api
    dockerfile: ./Dockerfile
    envVars:
      - key: PYTHONUNBUFFERED
        value: "true"
    health:
      path: /health
      checkIntervalSeconds: 30
